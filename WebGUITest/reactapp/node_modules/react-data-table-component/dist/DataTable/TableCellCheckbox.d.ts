import * as React from 'react';
import { RowRecord, RowState, SingleRowAction } from './types';
declare type TableCellCheckboxProps<T> = {
    keyField: string;
    row: T;
    rowCount: number;
    selected: boolean;
    selectableRowsComponent: 'input' | React.ReactElement;
    selectableRowsComponentProps: Record<string, unknown>;
    selectableRowDisabled: RowState<T>;
    onSelectedRow: (action: SingleRowAction<T>) => void;
};
declare function TableCellCheckbox<T extends RowRecord>({ keyField, row, rowCount, selected, selectableRowsComponent, selectableRowsComponentProps, selectableRowDisabled, onSelectedRow, }: TableCellCheckboxProps<T>): JSX.Element;
export default TableCellCheckbox;
